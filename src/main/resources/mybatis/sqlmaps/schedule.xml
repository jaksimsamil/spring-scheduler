<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.schedule">
	<select id="getScheduleDateList" resultType="com.spring.schedule.model.ScheduleDate">
		SELECT time_seq AS timeSeq, open_date AS openDate
		FROM pretask.spring_scheduler;
	</select>
</mapper>
